[pytest]
# Scope collection to the project's own suite. Without this, a bare `pytest`
# also picks up the agent's scratch artifacts at the repo root
# (test_solution.py, test_injection.py), which are LLM output, not project tests.
testpaths = tests
