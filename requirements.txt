# --- Core AI & LangGraph (Preserving your working versions) ---
langchain-google-genai==4.1.2
langchain-core==1.2.5
langgraph==1.0.5
google-genai==1.56.0
google-generativeai==0.8.6

# --- API Framework (NEW for v2.0) ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart

# --- Configuration & Validation ---
pydantic==2.12.5
pydantic-settings>=2.0.0

# --- Infrastructure (NEW for v2.0) ---
redis>=5.0.0
prometheus-client>=0.19.0

# --- Frontend (Keeping v1 UI for now) ---
streamlit==1.52.2

# --- Testing & Security ---
pytest==9.0.2
bandit      # Crucial for your SecOps feature (was missing in your list)
httpx       # Required for testing the API endpoints